# 💬 Manga Auto-Translator & Smart Thai Typesetter

เครื่องมือแปลอัตโนมัติฉบับสมบูรณ์สำหรับแปลการ์ตูนญี่ปุ่น/จาก อังกฤษเป็นไทย โครงการนี้มีสถาปัตยกรรม **Human-in-the-Loop (HITL)** ที่มั่นคง พร้อมด้วย **อัลกอริทึมการจัดการฟอนต์ไทยแบบอัจฉริยะ** ที่แก้ปัญหา Thai vowel/tone overlap ได้อย่างสมบูรณ์บนวินโดวส์โดยไม่พึ่งพา C-libraries ระดับ OS (เช่น `libraqm`)

---

## 🌟 ฟีเจอร์หลัก

- **✂️ Content-Aware Image Slicing (ตัดภาพแบบสมาธิ)**  
  ตรวจจับช่องว่างโดยอัตโนมัติและตัดแถบ webtoon ยาวๆ เป็นหน้าทีละหน้า โดยไม่ตัดผ่านข้อความหรือกล่องโต้วจริยากรรม

- **🧠 Contextual OCR Clustering (การจัดกลุ่ม OCR ตามบริบท)**  
  อัปเกรด EasyOCR จาก line-by-line reading เป็น Paragraph-level clustering แบบที่กำหนดเองเพื่อป้องกันการสูญหายของบริบท

- **⏸️ Human-in-the-Loop (HITL) Workflow (เวิร์กโฟลว์พร้อมการตรวจสอบมนุษย์)**  
  แยกกระบวนการสกัดข้อมูลและการแสดงผลออกจากกัน AI สร้าง `translations.json` ฉบับร่าง จากนั้นหยุดเพื่อให้ผู้แก้ไขสามารถปรับแต่งได้ก่อนแสดงผลสุดท้าย

- **🪄 Seamless Inpainting (การซ่อมแซมภาพแบบไม่มีรอยต่อ)**  
  ใช้ OpenCV Telea algorithm ลบข้อความเดิมออกจากกล่องโต้วได้อย่างสะอาด

- **🇹🇭 Smart Thai Typesetting (Strip & Shift Algorithm)**
  - การตัดคำฉบับที่กำหนดเองตามพจนานุกรม AI (`pythainlp`) เพื่อหลีกเลี่ยงการตัดคำที่ไม่เหมาะสม
  - คำนวณพิกัดอย่างชาญฉลาดเพื่อเลื่อน Thai tone marks (วรรณยุกต์) ขึ้นลงตามการมีอยู่ของสระ
  - บรรลุการพิมพ์แบบ pixel-perfect บนระบบปฏิบัติการใดๆ โดยไม่ต้องใช้ไลบรารี่ภายนอก

---

## 🛠️ Stack เทคโนโลยี

| องค์ประกอบ           | ไลบรารี่               | เวอร์ชัน            |
| ------------------- | -------------------- | ----------------- |
| **ภาษา**            | Python               | 3.11+             |
| **Computer Vision** | OpenCV               | `opencv-python`   |
| **OCR**             | EasyOCR              | ล่าสุด              |
| **การแปล**          | Google Translate API | `deep-translator` |
| **NLP (ไทย)**       | PyThaiNLP            | ล่าสุด              |
| **การประมวลผลภาพ**  | Pillow               | `PIL`             |
| **การคำนวณเชิงตัวเลข** | NumPy                | ล่าสุด              |

---

## 🚀 เริ่มต้นอย่างรวดเร็ว

### 1. ข้อกำหนดเบื้องต้น
- Python 3.11 ขึ้นไป
- ไฟล์ฟอนต์ภาษาไทย (เช่น `THSarabunNew Bold.ttf` หรือ `tahoma.ttf`)

### 2. ตั้งค่าสภาพแวดล้อม
```bash
# ติดตั้ง uv (Package manager ที่รวดเร็ว)
pip install uv

# สร้าง Virtual Environment ด้วย Python 3.11
uv python install 3.11
uv venv --python 3.11

# เปิดใช้งาน Virtual Environment
# บน Windows:
.venv\Scripts\activate
# บน macOS/Linux:
source .venv/bin/activate

# ติดตั้ง Dependencies
uv sync
```

### 3. ตั้งค่าฟอนต์
วางไฟล์ฟอนต์ไทยในโฟลเดอร์โครงการรูต และอัปเดตตัวแปร `font_path` ใน `main.py`:
```python
Typesetter(font_path="THSarabunNew Bold.ttf")
```

---

## 🕹️ เวิร์กโฟลว์: Pipeline 2 เฟส

### เฟส 1️⃣: AI Extraction & Drafting (สกัดข้อมูลและสร้างฉบับร่าง)
รันสคริปต์ครั้งแรก OCR จะ:
1. สแกนภาพ
2. จัดกลุ่มย่อหน้าด้วยตรรกะที่กำหนดเอง
3. สร้างการแปลฉบับร่าง
4. บันทึกผลลัพธ์ลงใน `translations.json`
5. **หยุดชั่วคราวเพื่อให้มนุษย์ตรวจสอบ**

```bash
uv run main.py
```

### เฟส 1.5️⃣: Human Review (ขั้นตอนหลักสำคัญ!)
เปิด `translations.json` ด้วยโปรแกรมแก้ไขโค้ด:
```json
{
  "id": 1,
  "original_text": "Hello world",
  "translated_text": "สวัสดีชาวโลก"  ← แก้ไขฟิลด์นี้
}
```
แก้ไขการแปลที่ดูเหมือนหุ่นยนต์ จากนั้นบันทึกไฟล์

### เฟส 2️⃣: Injection & Typesetting (ฉีดข้อมูลและจัดการฟอนต์)
รันสคริปต์อีกครั้ง มันจะ:
1. ตรวจจับ `translations.json` ที่มีอยู่
2. ข้าม OCR phase (เร็วขึ้น!)
3. ลบข้อความต้นฉบับด้วย inpainting
4. แสดงผลข้อความไทยที่จัดการฟอนต์อย่างสวยงาม

```bash
uv run main.py
# Output: result_final_pipeline.png
```

---

## 📁 โครงสร้างโครงการ

```
converter-ocr/
├── main.py                      # สคริปต์ pipeline หลัก
├── pyproject.toml               # การกำหนดค่าโครงการและ Dependencies
├── .gitignore                   # กฎการигнорирование Git
├── README.md                    # การอ้างอิงอย่างรวดเร็ว (ไฟล์นี้)
├── docs/
│   ├── README.md                # เอกสารภาษาอังกฤษ
│   ├── README_TH.md             # เอกสารภาษาไทย (ไฟล์นี้)
│   └── ARCHITECTURE.md          # Deep-dive ด้านเทคนิค (ทางเลือก)
├── image/
│   └── 02-optimized.webp        # หน้าตัวอย่าง Manga/Webtoon
├── output_tiles/                # Image tiles ที่สร้างขึ้น
└── translations.json            # HITL Checkpoint (ผู้ใช้แก้ไขที่นี่)
```

---

## 🧩 การปรับแต่งและการขยายคุณสมบัติ

โครงการนี้ปฏิบัติตามหลัก **SOLID** (Single Responsibility, Strategy Pattern) เพื่อให้ปรับแต่งได้ง่าย:

### 🔄 สลับเครื่องมือแปล
แทนที่ `GoogleTranslator` ใน `MangaOcrEngine` ด้วย:
- **OpenAI GPT-4o** สำหรับความเข้าใจบริบทที่ดีกว่า
- **Claude 3** สำหรับการแปลแบบละเอียดอ่อน
- **DeepL** สำหรับคุณภาพสูงขึ้น

**ตัวอย่าง:**
```python
class MangaOcrEngine:
    def __init__(self):
        self.translator = OpenAITranslator()  # สลับที่นี่
```

### 🎯 ปรับแต่งการจัดกลุ่ม OCR
ปรับค่า `vertical_gap` threshold หากกล่องถูกรวมกันเกินไป:
```python
# ใน MangaOcrEngine.extract_to_json_format()
if vertical_gap < 80:  # ← เพิ่มเป็น 120 สำหรับการจัดกลุ่มที่แน่นขึ้น
    # รวมกล่อง
```

### 📚 ปรับปรุงพจนานุกรม NLP
เพิ่มคำที่กำหนดเองสำหรับชื่ออักษรที่สอดคล้องกัน:
```python
# ใน Typesetter.__init__()
custom_words.update(["ชื่ออักษร", "ชื่ออื่น"])  # เพิ่มคำของคุณ
```

### 🎨 นำทางเครื่องมือทำความสะอาดแบบที่กำหนดเอง
สร้างกลยุทธ์การทำความสะอาดใหม่โดยไม่ทำลาย pipeline:
```python
class AIGenerativeFillCleaner(CleanStrategy):
    def clean(self, image, bbox):
        # ตรรมชาติ AI inpainting ของคุณ
        return cleaned_image
```

---

## 🐛 การแก้ไขปัญหา

### ปัญหา: ตัวอักษรไทยปรากฏซ้อนทับหรือไม่เรียงตัว
**วิธีแก้:** ตรวจสอบว่าคุณใช้ฟอนต์ไทยคุณภาพสูงที่รองรับตัวอักษรรวมกัน  
ฟอนต์ที่แนะนำ:
- Thai Sarabun New (ตั้งค่าเริ่มต้นของ Windows)
- Noto Sans Thai
- Prompt Regular

### ปัญหา: การจัดกลุ่ม OCR รวมกล่องข้อความที่ไม่เกี่ยวข้องเข้าด้วยกัน
**วิธีแก้:** ลด `vertical_gap` threshold หรือเพิ่ม `search_window` ใน `ContentAwareSlicer`

### ปัญหา: การแปลที่สร้างขึ้นดูเหมือนหุ่นยนต์/ไม่เป็นธรรมชาติ
**วิธีแก้:** ใช้ Phase 1.5 Human Review เพื่อปรับแต่งการแปล พิจารณาอัปเกรดเป็น GPT-4o เพื่อความเข้าใจบริบทที่ดีกว่า

### ปัญหา: `cv2.inpaint()` ทิ้งสิ่งเนื้อที่มองเห็น
**วิธีแก้:** สลับไปใช้ `SolidFillCleaner` เพื่อการลบข้อความที่สะอาดกว่า (แม้ว่าจะไม่ประมาณได้ดีเท่านั้น)

---

## 📊 เมตริกคุณสมบัติ

| การดำเนิน             | เวลา (ตัวอย่างภาพ 800x15993px) | หมายเหตุ                         |
| ------------------- | ---------------------------- | ------------------------------- |
| Image Slicing       | ~2-3วินาที                     | ContentAwareSlicer ที่มี 14 tiles  |
| OCR Extraction      | ~15-30วินาที                   | ขึ้นอยู่กับความซับซ้อนของภาพ           |
| Translation         | ~5-10วินาที                    | การประมวลผลแบบกลุ่มผ่าน Google API |
| Typesetting         | ~1-2วินาที                     | Rendering ต่อ Tile               |
| **Total (Phase 1)** | ~25-45วินาที                   | ครั้งแรกพร้อมการหยุดชั่วคราวของมนุษย์   |
| **Total (Phase 2)** | ~3-5วินาที                     | รันซ้ำด้วยการแปลแบบแคช              |

---

## 🤝 การมีส่วนร่วม

ยินดีต้อนรับการมีส่วนร่วม ปัญหา และคำขอฟีเจอร์!

**วิธีการมีส่วนร่วม:**
1. ทำ Fork ของที่เก็บ
2. สร้างสาขาฟีเจอร์: `git checkout -b feature/YourFeature`
3. Commit การเปลี่ยนแปลงของคุณ: `git commit -m 'Add YourFeature'`
4. Push ไปยังสาขา: `git push origin feature/YourFeature`
5. ส่ง Pull Request

**ไอเดียในการมีส่วนร่วม:**
- [ ] Web UI สำหรับเวิร์กโฟลว์ HITL
- [ ] รองรับคู่ภาษาอื่นๆ (Japanese→Thai, Korean→Thai)
- [ ] โหมดการประมวลผลแบบกลุ่ม
- [ ] การรวมเบอท Discord
- [ ] การลบข้อความขั้นสูงแบบ AI (Generative Fill)

---

## 📄 ใบอนุญาต

โครงการนี้ได้รับใบอนุญาตภายใต้ MIT License ดู `LICENSE` ไฟล์เพื่อรายละเอียด

---

## 🙏 ความขอบคุณ

- **EasyOCR** สำหรับ OCR หลายภาษาที่มั่นคง
- **PyThaiNLP** สำหรับการประมวลผลภาษาไทย
- **OpenCV** และ **Pillow** สำหรับการจัดการภาพ
- **Deep-Translator** สำหรับการรวมเอพีไอการแปลที่ไร้รอยต่อ

---

## 📞 การสนับสนุน

พบแบ็ก หรือมีคำถาม?
- เปิด [Issue](https://github.com/YourUsername/converter-ocr/issues)
- Discussion มาเร็วๆ!

**แปลสนุกสนาน!** 🎨✨
